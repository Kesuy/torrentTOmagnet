# v2.0.1

## 主要更新

- 修复安装 `.torrent` 文件右键菜单后，Explorer 可能把菜单显示成“打开 .torrent 文件”的问题。
- 右键菜单显式注册 `MUIVerb`，始终显示“种子转磁力链接”。
- 增加 `NeverDefault`，避免自定义转换命令被 Windows 当作 `.torrent` 的默认打开动作。
- 右键菜单增加程序图标，直接使用单文件 EXE 内嵌的 `icon.ico` 资源，无需额外图标文件。
- 安装或删除右键菜单后主动通知 Explorer 刷新文件关联缓存。
- 增加右键菜单注册回归测试。
